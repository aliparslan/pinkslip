<script lang="ts">
</script>

<span class="apple-mark" aria-hidden="true"></span>

<style>
  .apple-mark {
    width: 18px;
    flex-shrink: 0;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif;
    /* intentional: hand-tuned to render the Apple glyph at the right visual
       weight against surrounding text; uses the system font, not app tokens,
       so it isn't on the app's type scale by design. */
    /* stylelint-disable-next-line declaration-property-value-allowed-list */
    font-size: 21px;
    font-weight: 400;
    line-height: 1;
    text-align: center;
    transform: translateY(-0.5px);
  }
</style>
